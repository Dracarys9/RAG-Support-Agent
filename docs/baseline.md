# Baseline — Milestone 0

## Repository state

The working copy was cloned from `https://github.com/Dracarys9/RAG-Support-Agent.git` at commit `7372222` (`Initial commit`). The public repository initially contained only a title-only `README.md`; it did not contain an application, tests, setup files, or the supplied assignment data.

The assignment content was synchronized into the working copy without modifying the supplied knowledge-base Markdown files, order data, or visible evaluation cases. The exact assignment brief is retained outside the application source during this bootstrap step and will be reflected in the final README and documented project structure.

## Baseline checks

| Check | Result |
|---|---|
| Repository clone | Passed |
| Initial Git commit | `7372222` |
| Python runtime | Python 3.12.3 |
| Node runtime | Node.js v22.13.0 |
| Test discovery | No test configuration or application source exists yet |
| Baseline tests | Not runnable because no test suite exists yet |
| Credentials added | None |

## Initial constraints

The repository has no prescribed stack. The implementation should therefore favor a small, reproducible local system that can be demonstrated within the assignment timebox. The first application milestone will establish the project setup, dependency policy, environment template, and test runner before implementing retrieval or agent behavior.
